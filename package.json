{
  "name": "timewarp",
  "version": "1.1.0",
  "description": "🕰️ Deterministic time simulation and manipulation library for TypeScript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint 'src/**/*.{ts,js}'",
    "prepare": "npm run build",
    "prepublishOnly": "npm test",
    "version": "npm run build && git add -A dist",
    "release": "npm publish --access public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/beladevo/timewarp"
  },
  "keywords": [
    "time",
    "simulation",
    "typescript",
    "deterministic",
    "testing",
    "timewarp",
    "virtual time"
  ],
  "author": "Omri Beladev",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "ts-jest": "^29.4.0",
    "typescript": "^5.0.0"
  }
}
